general:
  artifacts:
    - "bin/wstunnel.tar.gz"
    - "bin/wstunnel_windows_x64.tar.gz"
    - "bin/wstunnel_windows_x86.tar.gz"
    - "bin/wstunnel_x86_64.tar.gz"

deployment:
  Release:
    branch: master
    commands:
      - ./deploy.sh

